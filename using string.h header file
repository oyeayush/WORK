#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char name[20];
    int len;
    printf("\nenter name:");
    gets(name);
    len = strlen(name);
    printf("\nname is : %s and length of string is %d",name,len);
    return 0;
}
